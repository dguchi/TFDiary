<div class="chat">
    <div class="chat-user">
        <% user = get_user(chat.user_id) %>
        <%= image_tag user.image, :size => "10x10" %>
        &emsp;
        <%= link_to user.name, user_path(user.id) %>
        &emsp;
        <%= simple_time(chat.updated_at) %>
        &emsp;
        <% if chat.user_id == current_user.id %>
            <%= link_to "削除", :controller => "group_member", :action => "delete_chat", :chat_id => chat.id %>
        <% end %>
    </div>
    <div class="chat-content">
        <%= simple_format(chat.content) %>
    </div>
</div>
