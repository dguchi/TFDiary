<h2>登録メニュー一覧</h2>
<p>
    <%= link_to "新規メニュー作成", new_menu_path %>
</p>
<div id="menu-index">
    登録メニュー数(<%= @menus.count %>)
    <% @menus.each do |menu| %>
        <div class="regist-menu">
            <%= link_to menu.name, menu_path(menu.id) %>
        </div>
    <% end %>
</div>